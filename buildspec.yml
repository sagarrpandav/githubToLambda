version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Installing Zero dependencies..."
  build:
    commands:
      - echo "Zipping zero deployment packages..."
  post_build:
    commands:
      - echo "Updating lambda Function..."
      - aws lambda update-function-code --function-name dynamicLambda --file fileb://lambda_function.py
      - echo "DONE!!"
